import { styled } from '@mui/material/styles';
import MuiAppBar from '@mui/material/AppBar';

const Navbar_AppBar = styled(MuiAppBar)(() => ({
  backgroundColor: '#B4E197',
  flex: '0 1 auto',
}));

export default Navbar_AppBar;
