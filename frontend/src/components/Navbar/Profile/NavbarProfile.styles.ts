import { styled } from '@mui/material/styles';
import MuiButton from '@mui/material/Button';

const Navbar_Profile_Button = styled(MuiButton)(() => ({
  color: 'white',
}));

export default Navbar_Profile_Button;
