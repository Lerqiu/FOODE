import { styled } from '@mui/material/styles';
import MuiBox from '@mui/material/Box';

export const OffersPage_MainContainer = styled(MuiBox)(() => ({
  flex: '1 1 auto',
  display: 'flex',
  flexFlow: 'column',
  backgroundColor: '#E9EFC0',
}));

export const OffersPage_SecondaryContainer = styled(MuiBox)(() => ({
  flex: '1 1 auto',
  display: 'flex',
}));
